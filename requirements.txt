# Stage 1 dependencies (ASCII-only; pip on Windows GBK cannot read UTF-8 comments)
pandas>=2.0
openai>=1.0
pyyaml>=6.0
python-dotenv>=1.0

# Stage 2 dependencies (conditional-prediction anomaly layer)
# CPU build:
#   pip install "torch>=2.0" --index-url https://download.pytorch.org/whl/cpu
# GPU server build (pick cuXXX to match the server CUDA version, e.g. cu121):
#   pip install "torch>=2.0" --index-url https://download.pytorch.org/whl/cu121
# Stage 2 auto-detects GPU by default (model.device="auto"); override via: --device cuda|cpu
torch>=2.0
numpy>=1.23

# Stage 2 multi-detector framework (statistical / categorical / neighbor / clustering / isolation-forest)
scikit-learn>=1.2
rapidfuzz>=3.0
